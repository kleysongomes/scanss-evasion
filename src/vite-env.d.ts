/// <reference types="vite/client" />

/**
 * Injetados em tempo de build por `vite.config.ts`.
 *
 * A versao vem do `package.json` e a data, do ultimo commit - assim o rodape do
 * site nao depende de ninguem lembrar de atualizar um numero na mao.
 */
declare const __VERSAO__: string
declare const __BUILD__: string
